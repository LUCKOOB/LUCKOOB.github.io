---
layout: post
tags: [overview, moonwalk]
---

큐(queue):FIFO(First-in First-out)규칙순서 알고리즘

- 삽입 `enqueue`
- 삭제 `dequeue`

------------------
 5 | -2 | 10 |
------------------

```py
enqueue(5)
enqueue(-2)
enqueue(10)
dequeue() # -5
dequeue() # -2
```
------------------
 10 |
------------------

`queue` 는 두가지 인덱스가 필요하다
'enqueue': 현재어디까지 쌓여있는 알수있어야한다'rear_index' 
'dequeue`: 다음에 나갈 인덱스를 알아야한다 `fornt_index` 

```py
class Queue:
  def __init__(self);
      self.items=[]
      self.fromt.index=0
  def enqueue(self,val);
      self.itmes,append(val)
  def dequeue(self,val);
      if self.fromt.index==len(self.items);
          print("'Q' is ematy")
      else;
          x=self.items[front.index]
          front_index+=1
          return = x
```          
        
